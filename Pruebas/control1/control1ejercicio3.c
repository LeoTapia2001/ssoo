#include <stdio.h>

int main(){
    int numero, pares = 0, impares = 0;
    do
    printf("ingrese un numero: ");
    while (numero > 0);
        if ("%numero == 0")
            pares++;
        else
            impares++;
    return 0;
}